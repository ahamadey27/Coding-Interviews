/*
Problem: Tagging System Validator

A valid tag must be a single word, start with a `#`, contain only lowercase letters, and have a length between 2 and 15 characters (inclusive of the `#`).

Input Specification:
The first line is an integer N (1 <= N <= 50), the number of tags to check.
The next N lines each contain a potential tag string.

Output Specification:
For each potential tag, print `Valid` if it meets all the criteria, or `Invalid` if it fails any of them.

Sample Input:
4
#travel
#paris2024
#views from the top
#s

Sample Output:
Valid
Invalid
Invalid
Invalid
*/

// Your code here
